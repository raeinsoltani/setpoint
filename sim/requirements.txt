# Core (needed to run the demo and tests)
numpy>=1.24
matplotlib>=3.7
PyYAML>=6.0
pytest>=7.0

# Optional — only for running against a real cluster (live mode):
# kubernetes>=28.0
#
# PrometheusCollector uses the Python standard library (urllib), so no HTTP
# client dependency is required.
